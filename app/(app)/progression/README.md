# progression/

Exercise progression chart page — plots planned vs actual weight/volume over time across mesocycles.

## Files
- `page.tsx` — `ProgressionPage` server component; fetches exercises, renders `ProgressionChart`
