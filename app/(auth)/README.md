# (auth)/

Route group for unauthenticated pages — no app shell or nav.

## Files
- `layout.tsx` — passthrough layout (no sidebar/header)
- `layout.test.tsx` — layout render test

## Subdirectories
- `login/` — login page rendering `LoginForm`
