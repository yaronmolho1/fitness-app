# progression/

Exercise progression data queries — compares planned vs actual weight and volume across logged workouts.

## Files
- `queries.ts` — `getProgressionData()`: fetches logged workout history for a canonical name, computes top-set weight, actual/planned volume per session with mesocycle context
