# exercises/

Exercise CRUD server actions and queries.

## Files
- `actions.ts` — `createExercise()` with zod validation + case-insensitive duplicate check, `deleteExercise()` with slot-usage protection
- `queries.ts` — `getExercises()` returns all exercises ordered by creation date desc
